# 在 Supabase 项目的 Project Settings → API 页面里找到这两个值。
# 本地开发时复制这个文件为 .env.local 并填入真实值。
# 部署到 Vercel 时，在 Vercel 项目的 Settings → Environment Variables 里填同样的两项。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
